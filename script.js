//setup runs initially and creates your canvas
function setup() {
    createCanvas(600, 600);
    background(150);
}

// draw runs consitently and draws your elements
function draw() {
    circle(300, 300, 120)
}